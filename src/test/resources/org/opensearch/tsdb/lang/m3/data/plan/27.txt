DIFF(keepNans=false,groupBy=[])
  TRANSFORM_NULL(0.0)
    SCALE(10.000000000000568)
      AS_PERCENT(groupBy=[])
        MOVING(1d, SUM)
          FETCH({name=[errors]}, !{})
        MOVING(1d, SUM)
          FETCH({name=[total]}, !{})
  SCALE(1000.0000000000568)
    AGG(MIN, groupBy=[])
      UNION
        TRANSFORM_NULL(0.0)
          SCALE(10.000000000000568)
            AS_PERCENT(groupBy=[])
              MOVING(24h, SUM)
                FETCH({name=[errors]}, !{})
              MOVING(24h, SUM)
                FETCH({name=[total]}, !{})
        TRANSFORM_NULL(0.0)
          SCALE(10.000000000000568)
            AS_PERCENT(groupBy=[])
              MOVING(2h, SUM)
                FETCH({name=[errors]}, !{})
              MOVING(2h, SUM)
                FETCH({name=[total]}, !{})
