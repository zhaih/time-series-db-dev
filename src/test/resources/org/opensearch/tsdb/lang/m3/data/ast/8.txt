ROOT
  PIPELINE
    PIPELINE
      GROUP
        FUNCTION(fetch)
          TAG_KEY(name)
            TAG_VALUE(queries)
        FUNCTION(sum)
    PIPELINE
      GROUP
        FUNCTION(fetch)
          TAG_KEY(name)
            TAG_VALUE(exceptions)
        FUNCTION(sum)
