fetch name:metric1 | sum
  | asPercent(
    (fetch name:metric2
      | sum
      | fetch name:metric3     # test dangling functions inside group
      | avg
      | (fetch name:metric4
        | sum
      )
      | avg
    )
  )
  | moving 5m min
