ALIAS(exec)
  SCALE(0.01)
    SCALE(100.0)
      AS_PERCENT
        MOVING(1m, SUM)
          TRANSFORM_NULL(0.0)
            AGG(SUM, groupBy=[dc])
              FETCH({uuid=[uuid-1, uuid-2, uuid-3], dc=[sfo*]}, !{})
        MOVING(1m, SUM)
          TRANSFORM_NULL(0.0)
            AGG(SUM, groupBy=[dc])
              FETCH({uuid=[uuid-1, uuid-2, uuid-3], dc=[sjc*]}, !{})
