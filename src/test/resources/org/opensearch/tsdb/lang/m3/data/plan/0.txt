ALIAS({{.region}})
  AGG(COUNT, groupBy=[])
    VALUE_FILTER(GE, 5.0)
      KEEP_LAST_VALUE()
        AGG(MAX, groupBy=[])
          AGG(MIN, groupBy=[region])
            AGG(AVG, groupBy=[])
              AGG(SUM, groupBy=[region])
                MOVING(1m, SUM)
                  TRANSFORM_NULL(0.0)
                    FETCH({name=[service.errors], region=[us-*, ca]}, !{})
