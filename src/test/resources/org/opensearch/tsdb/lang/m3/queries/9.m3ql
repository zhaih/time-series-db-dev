a = fetch name:a;
b = fetch name:b;
c = fetch name:c;
d = fetch name:d;

e = a | asPercent(b); # test chained macros in a single coordinator
f = c | asPercent(d);
e | asPercent(f) | alias three word alias # test alias by multiple words
