MOVING(5m, MIN)
  AS_PERCENT
    AGG(SUM, groupBy=[])
      FETCH({name=[metric1]}, !{})
    AGG(AVG, groupBy=[])
      UNION
        AGG(AVG, groupBy=[])
          AS_PERCENT
            AGG(SUM, groupBy=[])
              FETCH({name=[metric2]}, !{})
            FETCH({name=[metric3]}, !{})
        AGG(SUM, groupBy=[])
          FETCH({name=[metric4]}, !{})
